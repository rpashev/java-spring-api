spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: jdbc:postgresql://localhost:5432/unifiedapi_dev
    username: rpashev
    password: password
    driver-class-name: org.postgresql.Driver

  # JPA/Hibernate
  jpa:
    hibernate:
      ddl-auto: validate   # auto-create/update tables in dev
    show-sql: true        # show SQL statements in console

server:
  port: 8080

logging:
  level:
    root: INFO
    com.rpashev.api: DEBUG
    org.springframework.web: INFO
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

